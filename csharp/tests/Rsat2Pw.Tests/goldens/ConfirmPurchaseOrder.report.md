# Conversion report: Confirm purchase order

Generated by `rsat2pw`. Regenerate this alongside the spec whenever
the recording or the mapping table changes.

## Coverage

| | |
|---|---|
| Actions | 32 |
| Translated | 30 (93.8%) |
| Skipped | 1 |
| Not translated | 1 |
| Test cases | 2 (from fixtures/ConfirmPurchaseOrder-params.xlsx [Parameters]) |

## Translated

| Emitted call | Count |
|---|---:|
| `d365.click()` | 2 |
| `d365.closeForm()` | 1 |
| `d365.commitLookup()` | 2 |
| `d365.filter()` | 1 |
| a comment | 2 |
| `d365.navigate()` | 1 |
| `d365.openLookup()` | 2 |
| `d365.openRow()` | 1 |
| `d365.selectRow()` | 2 |
| `d365.selectTreeItem()` | 1 |
| `d365.setField()` | 2 |
| `d365.setGridCell()` | 1 |
| `d365.shortcut()` | 1 |
| `d365.tab()` | 1 |
| `test.step()` | 4 |
| `d365.withForm()` | 6 |

## Skipped

Recorded, understood, and deliberately not replayed: client-internal bookkeeping
with no user-visible effect. Each one still leaves a comment in the generated spec.

| Action | Count |
|---|---:|
| `CommandUserAction:GetFilters` | 1 |

## Not translated

Each heading is a Task Recorder action with no rule in `src/lower.rs`.
The properties are everything the recorder supplied, which is what a new
mapping rule keys off.

### `CommandUserAction:SelectForAdd` — 1 occurrence

| Property | Example value |
|---|---|
| `CommandName` | `SelectForAdd` |
| `ControlName` | `Grid` |
| `ControlType` | `Grid` |
| `Description` | `Select Grid to add a field to it.` |
| `ReturnType` | `Void` |
| `Sequence` | `43` |

## Test data

| Variable | Used by an action | In test data | Recorded default |
|---|---|---|---|
| `PurchId` | yes | yes | `000123` |
| `PurchTable_DeliveryDate` | yes | yes | `9/30/2026` |
| `PurchLine_PurchQty` | yes | yes | `12` |
| `PurchParmTable_Printout` | yes | yes | `true` |
| `ctrlFormTree` | no | yes | `ALL (ALL)\Office supplies (Office supplies)` |

## Translation outline

The recording in order. `!!` marks an action that was not translated, `~~` one
that was deliberately skipped.

```
   test.step Find the purchase order
     navigate PurchTableListPage (Display)
     withForm PurchTable
~~     skipped CommandUserAction:GetFilters (opens the filter pane; filter() does that itself)
       filter PurchId Is params.PurchId
       selectRow Grid[2]
       openRow Grid
   test.step Check the delivery details
     marker The delivery date must fall inside the open period.
     marker Sub-task: Delivery details. (Begin)
     withForm PurchTable
       shortcut ViewEdit
       tab PurchaseTab
       setField PurchTable_DeliveryDate = params.PurchTable_DeliveryDate
       openLookup PurchTable_DlvMode
       withForm PurchTable_DlvMode_Lookup
         selectRow LookupGrid[1]
       commitLookup PurchTable_DlvMode
       setGridCell PurchLineGrid[0].PurchLine_PurchQty = params.PurchLine_PurchQty
       setField PurchParmTable_Printout = params.PurchParmTable_Printout
   test.step Set the procurement category
     withForm PurchTable
       openLookup PurchLine_ProcurementCategory
       withForm EcoResCategorySingleLookup
         selectTreeItem ctrlFormTree <- params.ctrlFormTree
       commitLookup PurchLine_ProcurementCategory
   test.step Confirm the order
     withForm PurchTable
       click PurchFormLetter_PurchOrderJournal (MenuItemButton)
!!     unsupported CommandUserAction:SelectForAdd
       click Yes (CommandButton)
       closeForm
```
