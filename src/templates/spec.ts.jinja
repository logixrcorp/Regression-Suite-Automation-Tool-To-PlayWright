// ---------------------------------------------------------------------------
// Generated by rsat2pw from the Task Recorder recording "{{ recording_name }}".
// {{ action_count }} action(s){% if todo_count %}, {{ todo_count }} unmapped - search for TODO(rsat2pw){% endif %}.
//
// Do not hand-edit: regenerate instead. Durable changes belong in the runtime
// helper (runtime/d365.ts), which is where every D365 quirk is handled.
// ---------------------------------------------------------------------------

import { test } from '@playwright/test';
import { D365 } from '../runtime/d365';
import { cases } from '{{ data_module }}';

for (const params of cases) {
  test(`{{ test_title }} [${params.__case}]`, async ({ page }) => {
    const d365 = await D365.open(page);

{{ body }}
  });
}
